// next.config.js
//
// Configuration file for Next.js.
// Next.js automatically reads this file when it starts.
//
// We do not need any special configuration for Phase 1.
// This file exists so Next.js does not complain about a missing config,
// and so we have a place to add settings in later phases
// (environment variables, image domains, API rewrites, etc.)

/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;